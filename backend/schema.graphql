	type Query {
		time(time: Int!): Time
		sanityCheck: String!
  	}

  	type Time {
		value: Int!
		stop (name: String!): Stop!
	}

	type Stop {
		name: String!
		buses: [Bus!]!
	}

	type Bus {
		route: String
		arrivals: [Int]
	}